{
  "name": "openapi-scheme-parser",
  "version": "1.0.0",
  "description": "Parser for openapi config files in JSON format to convert the components to development ready interfaces",
  "bin": {
    "openapi-scheme-parser": "bin/index.js"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "test": "npm run build && ./bin/index.js"
  },
  "files": [
    "src",
    "bin",
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mbeutner/openapi-scheme-parser"
  },
  "bugs": {
    "url": "https://github.com/mbeutner/openapi-scheme-parser/issues"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@types/node": "^18.15.3",
    "openapi-types": "^12.1.0",
    "yargs-parser": "^21.1.1"
  }
}
